<svg viewBox="-18 -18 58 58" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <style>
      .idle-body {
        transform-origin: 11px 18px;
        animation: idle-breathe 3.2s ease-in-out infinite, idle-sway 6s ease-in-out infinite;
        animation: wk-bob 0.6s cubic-bezier(0.36,0.07,0.19,0.97) infinite;
      }
      .idle-eye {
        transform-origin: 11px 11px;
        animation: idle-blink 4s ease-in-out infinite;
      }
      .idle-shadow {
        transform-origin: 11px 22.5px;
        animation: idle-shadow-pulse 3.2s ease-in-out infinite;
      }
      .think-bubble { animation: think-bubble-float 3s ease-in-out infinite; }
      .zzz text { animation: zzz-float 2s ease-in-out infinite; }
      @keyframes idle-breathe {
        0%,100% { transform: scaleY(1) translateY(0); }
        50%     { transform: scaleY(0.97) translateY(0.3px); }
      }
      @keyframes idle-sway {
        0%,100% { transform: rotate(0deg); }
        25%     { transform: rotate(1.2deg); }
        75%     { transform: rotate(-1.2deg); }
      }
      @keyframes idle-blink {
        0%,8%,100% { transform: scaleY(1); }
        4%          { transform: scaleY(0.08); }
      }
      @keyframes idle-shadow-pulse {
        0%,100% { transform: scaleX(1); opacity: 0.35; }
        50%     { transform: scaleX(0.96); opacity: 0.28; }
      }
      @keyframes wk-bob {
        0%,100% { transform: translateY(0); }
        50%     { transform: translateY(-1.5px); }
      }
      @keyframes happy-bounce {
        0%,100% { transform: translateY(0) rotate(0); }
        50%     { transform: translateY(-3px) rotate(3deg); }
      }
      @keyframes think-bubble-float {
        0%,100% { transform: translateY(0); opacity: 0.8; }
        50%     { transform: translateY(-2px); opacity: 1; }
      }
      @keyframes zzz-float {
        0%,100% { transform: translateY(0); opacity: 0.3; }
        50%     { transform: translateY(-3px); opacity: 0.8; }
      }
    </style>
  </defs>
  <ellipse class="idle-shadow" cx="11" cy="22.5" rx="4" ry="0.6" fill="#c0c0c0"/>
  <g class="idle-body">
    <!-- hat --><polygon points="11,0 16,8 6,8" fill="#4455AA"/><polygon points="11,-1 12.5,4 9.5,4" fill="#5566CC"/><!-- stars --><circle cx="9" cy="4" r="0.5" fill="#FFD700"/><circle cx="13" cy="3" r="0.4" fill="#FFD700"/><circle cx="11" cy="2" r="0.6" fill="#FFD700"/><!-- robe bottom --><polygon points="5,18 17,18 15,22 7,22" fill="#6B4E97"/><!-- staff --><line x1="18" y1="4" x2="18" y2="20" stroke="#8B7355" stroke-width="1"/><circle cx="18" cy="3" r="1.5" fill="#FFD700" opacity="0.7"/>
    <rect x="5" y="6" width="12" height="12" rx="6" fill="#7B5EA7"/>
    <g class="idle-eye">
      <rect x="9" y="10" width="2" height="2" fill="#FFFFFF"/>
      <rect x="12" y="10" width="2" height="2" fill="#FFFFFF"/>
    </g>
    <path d="M9 14 Q11 16 13 14" stroke="#FFFFFF" stroke-width="0.8" stroke-linecap="round" fill="none"/>
  </g>
  
</svg>
